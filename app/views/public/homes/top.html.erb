<%= render 'public/items/genre_search', genres: @genres %>

<div class="col-9">
  <div class="text-center mb-4">
    <p>ようこそ、ながのCAKEへ！</p>
    <p>このサイトは、ケーキ販売のECサイトになります。</p>
    <p>会員ではない方も商品の閲覧はできますが、</p>
    <p>購入には会員登録が必要になります。</p>
  </div>

  <h3>新着商品</h3>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-3">
        <%= link_to public_item_path(item.id) do %>
          <p><%= image_tag item.get_image(100,100) %></p>

          <p><%= item.name %></p>

          <p>￥<%= item.price.to_s(:delimited) %></p>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="row">
    <%= link_to 'すべての商品を見る＞', public_items_path, class:"ml-auto" %>
  </div>
</div>
