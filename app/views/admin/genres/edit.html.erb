<div class="col">
  <h2 class="p-2 bg-secondary text-white mb-5 text-center">ジャンル編集</h2>

  <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
    <p>ジャンル名</p>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存', class:"btn btn-success" %>
  <% end %>
</div>